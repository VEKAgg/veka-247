# veka-247 — infrastructure reference
# Copy to .env if you want to use these values in scripts:
#   cp .env.example .env

# Your stream keys are configured in the ffplayout web UI (http://<VM_IP>:8787)
# Settings → Playout → Output → set mode to "stream" and paste:
#   rtmp://live.twitch.tv/app/YOUR_TWITCH_KEY
# Never put stream keys in this file or any file that gets committed.

# ── Infrastructure ────────────────────────────────────────────────────────────
TRUENAS_IP=192.168.1.98
VM_IP=192.168.1.6
CLIP_DIR=/srv/clips
