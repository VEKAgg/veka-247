# veka-247 rta branch - environment reference
# Stream keys are now managed via the Restreamer web UI (http://<VM_IP>:8080)
# Do NOT put stream keys in this file.

# Infrastructure
TRUENAS_IP=192.168.1.98
TRUENAS_DATASET=/mnt/tank/247Live
VM_IP=192.168.1.6
EOFP_INGEST_PORT=1935

# Clip directory (mounted from TrueNAS via NFS)
CLIP_DIR=/srv/clips

# Docker stack
FFPLAYOUT_PORT=8787
RESTREAMER_PORT=8080
RTMP_INGEST_PORT=1935
